---
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pgrubic-online-backend
    restart: always
    ports:
      - "8000:8000"
    environment:
      HOST_BIND: ${HOST_BIND:-backend}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost"]}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    networks:
      - pgrubic-online-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: pgrubic-online-frontend
    restart: always
    ports:
      - "80:80"
    environment:
      API_HOST: ${API_HOST:-http://backend:8000}
    depends_on:
      - backend
    networks:
      - pgrubic-online-network

networks:
  pgrubic-online-network:
    driver: bridge
    name: pgrubic-online-network
